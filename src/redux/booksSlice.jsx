import { createSlice } from '@reduxjs/toolkit'

export const booksSlice = createSlice({
    name:'books',
    initialState:{
       
    },
    reducers:{

    }

})
// export const {functions} = bookSlice.actions;
export default booksSlice.reducer;